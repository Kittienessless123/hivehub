/* getDaysInMonth(year: number, month: number): number — сколько дней в месяце (учёт февраля)

getMonthName(month: number, locale?: string): string — название месяца по номеру

getWeekDayNames(locale?: string, format?: 'short' | 'long'): string[] — массив названий дней

isValidDate(year: number, month: number, day: number): boolean — проверка существования даты */