/* formatDate(date: Date | null, format?: string): string — дату в строку (первая версия: только DD.MM.YYYY)

parseDate(dateString: string, format?: string): Date | null — строку в дату (первая версия: строгий DD.MM.YYYY) */