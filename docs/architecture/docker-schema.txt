┌─────────────────────────────────────────────────────────┐
│                    Docker Compose                        │
├─────────────────────────────────────────────────────────┤
│  Контейнер        Порт        Назначение                │
│  ──────────────   ────────    ──────────────────────    │
│  postgres-auth    5433/tcp    Auth & Reference DB       │
│  postgres-main    5432/tcp    User & Business DB        │
│  redis            6379/tcp    Кэш, сессии               │
│  rabbitmq         5672/tcp    Очереди сообщений         │
│  rabbitmq-mgmt    15672/tcp   RabbitMQ UI               │
│  api-gateway      3000/tcp    Входная точка             │
│  auth-service     3001/tcp    Аутентификация            │
│  user-service     3002/tcp    Профили пользователей     │
│  reference-service 3010/tcp   Справочные данные         │
└─────────────────────────────────────────────────────────┘